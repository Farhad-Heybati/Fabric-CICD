# Lakehouse bindings and workspace ID replacements are now auto-detected from the
# repository by deploy_test.py (see build_lakehouse_mapping / generate_parameter_yaml).
# Add custom find_replace rules here only if you need overrides beyond what is
# automatically generated.
#
# find_replace:
#   - find_value: "some-literal-value"
#     replace_value:
#       test: "replacement"
#     item_type: "Notebook"
